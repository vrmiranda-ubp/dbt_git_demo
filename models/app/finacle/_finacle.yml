sources:
  - name: finacle
    database: "{{ env_var('DBT_FINACLE_DB') }}"
    schema: "{{ env_var('DBT_FINACLE_TGT') }}"    
    loader: QLIK # informational only (free text)

    tables:
      - name: stg_finacle_orders
      - name: stg_finacle_customer
      - name: stg_finacle_lineitem
      - name: stg_finacle_supplier   
      - name: stg_finacle_part    
      - name: stg_finacle_partsupp
      - name: stg_finacle_nation
      - name: stg_finacle_region
      
