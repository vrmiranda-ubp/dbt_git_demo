sources:
  - name: finacle
    database: "{{ env_var('DBT_FINACLE_DB') }}"
    schema: "{{ env_var('DBT_FINACLE_TGT') }}"    
    loader: QLIK # informational only (free text)

    tables:
      - name: orders
      - name: customer
      - name: lineitem
      - name: supplier   
      - name: part    
      - name: partsupp
      - name: nation
      - name: region
      
