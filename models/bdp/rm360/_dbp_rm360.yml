sources:
  - name: rm360
    database: "{{ env_var('DBT_RM360_DB') }}"
    schema: "{{ env_var('DBT_RM360_SCHEMA') }}"
    loader: QLIK # informational only (free text)

    tables:
      - name: bdp_rm360_order_summary_fulfilled